<UserControl x:Class="PS3TrophiesIsPerfect.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="0,8,0,0">
        <Border CornerRadius="12" Background="{StaticResource Card}"
                BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
            <DataGrid x:Name="GamesGrid" Style="{StaticResource ListGrid}" ItemsSource="{Binding MyGames}"
                      MouseDoubleClick="GamesGrid_DoubleClick">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{StaticResource Text}" />
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Game" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Trophies" Binding="{Binding CountText}" Width="200" />
                    <DataGridTemplateColumn Header="Completion" Width="280">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center" Margin="4,0,12,0">
                                    <TextBlock Text="{Binding PercentText}" FontSize="12" Margin="0,0,0,5" />
                                    <ProgressBar Value="{Binding Percent}" Maximum="100" Height="4"
                                                 Foreground="{StaticResource Accent}" Background="#2A2A2A" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
