<Application x:Class="PS3TrophiesIsPerfect.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemeResources RequestedTheme="Dark" />
                <ui:XamlControlsResources />
            </ResourceDictionary.MergedDictionaries>

            <!-- Brand palette (P3P: vivid electric blue + chrome over deep OLED black, matching the banner). -->
            <SolidColorBrush x:Key="Accent" Color="#2A8BFF" />
            <SolidColorBrush x:Key="AccentGlow" Color="#5FA8FF" />
            <SolidColorBrush x:Key="Surface" Color="#08090C" />
            <SolidColorBrush x:Key="Card" Color="#13151A" />
            <SolidColorBrush x:Key="CardBorder" Color="#23272F" />
            <SolidColorBrush x:Key="Text" Color="#F0F2F5" />
            <SolidColorBrush x:Key="TextMuted" Color="#8A909A" />
            <SolidColorBrush x:Key="HeaderBg" Color="#13161C" />
            <SolidColorBrush x:Key="SelectionBg" Color="#15294A" />

            <!-- Metal colours for the trophy-type pill. -->
            <SolidColorBrush x:Key="Metal.P" Color="#6FC8F0" />
            <SolidColorBrush x:Key="Metal.G" Color="#F0C440" />
            <SolidColorBrush x:Key="Metal.S" Color="#C4CCD4" />
            <SolidColorBrush x:Key="Metal.B" Color="#CD7F32" />

            <!-- Shared across the shell window and the tab views (UserControls can't see Window.Resources). -->
            <BooleanToVisibilityConverter x:Key="B2V" />

            <Style x:Key="ToolButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="12,7" />
                <Setter Property="Margin" Value="0,0,4,0" />
            </Style>

            <!-- Custom window caption buttons (min / max-restore / close). Tag = hover colour. -->
            <Style x:Key="CaptionButton" TargetType="Button">
                <Setter Property="Width" Value="46" />
                <Setter Property="Height" Value="32" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{StaticResource Text}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Tag">
                    <Setter.Value><SolidColorBrush Color="#2A2A2A" /></Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background"
                                            Value="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource CaptionButton}">
                <Setter Property="Tag">
                    <Setter.Value><SolidColorBrush Color="#C42B1C" /></Setter.Value>
                </Setter>
            </Style>

            <!-- Clickable card with a subtle hover highlight. -->
            <Style x:Key="CardHover" TargetType="Border">
                <Setter Property="Background" Value="#1C1C1C" />
                <Setter Property="BorderBrush" Value="{StaticResource CardBorder}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="10" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#242424" />
                        <Setter Property="BorderBrush" Value="#3A3A3A" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Image that fades in when its bound source first arrives (bind with NotifyOnTargetUpdated=True). -->
            <Style x:Key="FadeImage" TargetType="Image">
                <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality" />
                <Style.Triggers>
                    <EventTrigger RoutedEvent="Binding.TargetUpdated">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.35" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Style.Triggers>
            </Style>

            <!-- Explicit dark header/cell styling so the grids stay readable regardless of ambient theme. -->
            <Style x:Key="DarkHeader" TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{StaticResource HeaderBg}" />
                <Setter Property="Foreground" Value="{StaticResource TextMuted}" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Padding" Value="10,8" />
                <Setter Property="BorderBrush" Value="{StaticResource CardBorder}" />
                <Setter Property="BorderThickness" Value="0,0,0,1" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
            </Style>
            <Style x:Key="DarkCell" TargetType="DataGridCell">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{StaticResource Text}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridCell">
                            <Border Background="{TemplateBinding Background}" Padding="0">
                                <ContentPresenter VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{StaticResource SelectionBg}" />
                        <Setter Property="Foreground" Value="{StaticResource Text}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="DarkRow" TargetType="DataGridRow">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{StaticResource Text}" />
            </Style>
            <Style x:Key="ListGrid" TargetType="DataGrid">
                <Setter Property="AutoGenerateColumns" Value="False" />
                <Setter Property="IsReadOnly" Value="True" />
                <Setter Property="Background" Value="{StaticResource Card}" />
                <Setter Property="Foreground" Value="{StaticResource Text}" />
                <Setter Property="RowStyle" Value="{StaticResource DarkRow}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="RowBackground" Value="Transparent" />
                <Setter Property="GridLinesVisibility" Value="None" />
                <Setter Property="HeadersVisibility" Value="Column" />
                <Setter Property="CanUserResizeRows" Value="False" />
                <Setter Property="SelectionMode" Value="Single" />
                <Setter Property="RowHeight" Value="52" />
                <Setter Property="RowHeaderWidth" Value="0" />
                <Setter Property="Margin" Value="6" />
                <Setter Property="ColumnHeaderStyle" Value="{StaticResource DarkHeader}" />
                <Setter Property="CellStyle" Value="{StaticResource DarkCell}" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
