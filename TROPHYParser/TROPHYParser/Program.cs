namespace TROPHYParser
{
    /// <summary>
    /// Unused entry point. TROPHYParser is consumed as a library by the trophy
    /// editor; this empty Main exists only because the project is built as an
    /// executable. Ad-hoc parsing checks can be wired up here (e.g. via the
    /// parsers' PrintState() helpers).
    /// </summary>
    class Program
    {
        static void Main(string[] args)
        {
        }
    }
}
